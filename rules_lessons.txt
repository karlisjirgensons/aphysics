All tasks and texts should be in latvian for latvian students.
All content and symbols should be presented as it is costumed in latvian standarts.

Fraction Formatting: When writing a fraction formatted as a/b, display it in standard vertical fraction notation (with numerator a on top and denominator b on the bottom), rather than inline text. It must be examples (uzdevumi) and also in theory.

Responsive Layouts: Ensure all slides are fully responsive across both desktop and mobile.

PC View: Maintain standard wide presentation layouts.

Mobile View: Use media queries (@media (max-width: 768px)) or fluid flexbox/grid containers so elements stack vertically, font sizes adjust dynamically, and content never overflows or requires horizontal scrolling on small screens.

make shure that square root expressions '√a; √(a + b); ...' look like in Latvian standart, it means that for example '√(a + b)' all expression 'a + b' is under square root.

Must use SRP and DRY principles.
